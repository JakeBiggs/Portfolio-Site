<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
   
    <script type="text/javascript" src="https://code.jquery.com/jquery-1.7.1.min.js"></script>

    <title>Projects</title>
</head>

<body id="bg-img">

    <main id="projects">
        <h1 class="lg-heading">
            My <span class="span-text-secondary">Projects</span>
        </h1>
        <h2 class="sm-heading">Check out some of my work...</h2> 
  <hr class="border border-3 opacity-75">
    <div class="project-list">
  
    </div >
   {{#each projectsData}}
    <div class="row">
        <div class="col-md-6">
            <div id="carousel{{@index}}" class="carousel slide" data-bs-interval="2000">
                <div class="carousel-inner">
                {{#each images}}
                {{#if @first}}
                    <div class="carousel-item active">
                        <img src="{{this.url}}" class="d-block w-100" alt="{{this.alt}}">
                    </div>
                {{else}}
                    <div class="carousel-item">
                        <img src="{{this.url}}" class="d-block w-100" alt="{{this.alt}}">
                    </div>
                {{/if}}
                

                {{/each}}
                </div>
                <button class="carousel-control-prev" type="button" data-bs-target="#carousel{{@index}}" data-bs-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Previous</span>
                </button>
                <button class="carousel-control-next" type="button" data-bs-target="#carousel{{@index}}" data-bs-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Next</span>
                </button>
            </div>
        </div>

        <div class="col-md-6 d-flex align-items-center">
            <div class="card mb-3">
                <div class="card-header"><h5 class="card-title">{{title}}</h5></div>
                <div class="card-body">
                    
                    <p class="card-text">{{description}}</p>
                    <a href="{{git}}" class="btn btn-standard">Github Page</a>
                </div>
                <div class="card-footer">
                    {{#each skills}}
                    <span class="badge badge-pill bg-tertiary">{{this}}</span>
                    {{/each}}
                </div>
            </div>
        </div>
    </div>
    
    <hr class="border border-3 opacity-75">
    
    {{/each}}
    </main>

    <script src="/javascripts/carousel.js"></script>
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js" type="text/javascript"></script>

   

</body>

</html>